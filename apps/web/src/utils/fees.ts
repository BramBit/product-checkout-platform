/**
 * Fees mirror configuration for frontend calculations before transaction creation.
 * NOTE: These constants MUST match the environment variables / backend defaults:
 * BASE_FEE_CENTS (default 500000 = 5.000 COP)
 * DELIVERY_FEE_CENTS (default 800000 = 8.000 COP)
 */
export const BASE_FEE_CENTS = 500000;
export const DELIVERY_FEE_CENTS = 800000;
